- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.config/nvim:
    ~/.config/sway:
    ~/.config/waybar:
    ~/.config/rofi:
    ~/.config/foot:

- shell:
  - [git submodule update --init --recursive, installing submodules]
